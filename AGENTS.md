# AGENTS.md

## Shell Commands

- Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk npm run build
rtk npm test
```

## Workflow

- Keep permanent project instructions in this file so Codex and Claude Code share the same guidance.
- When ending a session, summarize any new durable rules here instead of leaving them only in chat.
